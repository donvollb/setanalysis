#### Funktionen ####

## generell
- Alternative für minipage in Quarto (latexunabhängig)
- verschachtelte if-else Bedingungen ein wenig auflösen und entschärfen (z.B. mit else if oder return/stop)
- wenn nicht schon geschehen: Alles nicht direkt offensichtliche auskommentieren
- Bonus: größere for-Schleifen vektorisieren

## Was wollen wir?
- eine Datei pro Funktion
- Grunddoku: Argumente beschreiben
- # notrun Beispiel
- Nice to have: Beispieldaten/Datensätze
- Nice to have: automatisierte Tests?

## konkrete Funktionen

# subchunkify:
- prüfen, ob Alternativen
- quarto-Syntax einarbeiten

# get.label
- übertragen

# lv.kable
- noch Argumente überprüfen: Ist alles integriert?
- in lv.table umbenennen
- andere Lösung für defaults

# aggr.data
- vielleicht eleganter, aber kann man so lassen

# NEU? inkl.checks für wrapper Funktionen?
- vielleicht, erstmal nicht wichtig

# Funktionen für Beispiele und Legenden: so lassen

# Tabellen-Funktionen
- generell durchschauen, vielleicht eleganter möglich?
- Latex-Zeug entfernen
- vielleicht besser die colwidths regeln


# evasys.sk.plot
- einfach mal drüberschauen

# Bisherige Unzulänglichkeiten merge_many

- merge.multi.sk funktioniert theoretisch, aber praktisch bisher nutzlos, weil manuelle Eingabe der Überschriften nicht richtig möglich
- merge.multi.subj lässt sich nicht anhand des Fragentyps erkennen, funktioniert bisher nicht
- merge.wl wird bisher auch nicht erkannt
- merge.num funktioniert zwar grundsätzlich, aber da eigentlich eine Angabe des Labels für den Plot notwendig ist, ist es nicht so toll, außerdem wird da hier die Labelsetzung und -beschriftung allerdings bisher manuell erfolgt ist es nicht richtig nützlich

# Ideale Umbennennung der Funktionen (Vorschlag Simon)

  merge_num	    <- merge.num & merge.fachsem
✓ merge_open	<- merge.open & open.answers
  merge_mc	    <- merge.mc
  merge_subj	<- merge.subj
  merge_wl	    <- merge.wl
✓ merge_grade	<- grade
  merge_rueck	<- boxplot.ruecklauf
  merge_aggr  <- merge.multi.sk
  merge_evasys  <- merge.evasys.sk

barplot_num 	<- barplot.freq
barplot_choice	<- barplot.sc.mc
barplot_evasys  <- evasys.skala.plot
boxplot_grade	<- boxplot.gesnote
boxplot_aggr	<- boxplot.aggr.sk?
boxplot_wl		<- boxplot.workload
boxplot_rueck	<- Teile von boxplot.ruecklauf

aggr_data <- aggr.data
text.alternative: löschen und durch etwas Code im ShowUp-Bericht ersetzen (dafür ist keine Funktion nötig)